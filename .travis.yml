language: java

jdk:
  - oraclejdk8

notifications:
  email: false

install:
  - ./gradlew setupCIWorkspace -S

script:
  - ./gradlew build -S

env:
  global:
    TERM=dumb

sudo: false
cache:
  directories:
  - $HOME/.gradle/caches
  - $HOME/.gradle/wrapper

git:
  depth: 1