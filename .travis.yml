sudo: false

notifications:
  email: false

git:
  depth: 1

language: java

jdk:
  - oraclejdk8

before_install:
  - chmod +x gradlew

install:
  - ./gradlew setupCIWorkspace -S

script:
  - ./gradlew build -S

cache:
  directories:
  - $HOME/.gradle/caches
  - $HOME/.gradle/wrapper